import { TImage } from "./common";

export type TSustainabilityReport = {
  titleWhite: string;
  titleGreen: string;
  subTitle: string;
  image: TImage;
  mobileImage: TImage;
  buttonText: string;
};
